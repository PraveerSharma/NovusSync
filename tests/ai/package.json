{
  "name": "@novussync/offline-ai-tests",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint . --max-warnings=0",
    "typecheck": "tsc -p tsconfig.json",
    "test": "node --test offline-ai-safety.test.ts"
  },
  "dependencies": {
    "@novussync/config": "workspace:*"
  }
}
