version: "3.9"

services:
  # Database stack is not started yet in this initial skeleton.
  # Add local PostgreSQL + seed harness during FND-05 rollout.
  # Placeholder intentionally intentionally empty to keep foundation local-only.
  placeholder:
    image: alpine:3.20
    command: ["sh", "-c", "sleep infinity"]
