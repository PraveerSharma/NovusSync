# Redacted-Handoff Checker Fixtures

Every CSV in this directory is fictional regression-test input. These files use `TRN-` codes, contain no customer evidence, and must never be imported into the research denominator or cited as Gate G0 evidence.

Run the dependency-free suite from the repository root:

```sh
ruby scripts/discovery/test_analyze_handoff.rb
```

